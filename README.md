# Telegram Bot для управления бартерным каналом блоггеров

Telegram бот для автоматизации публикаций бартерных предложений в канале для блоггеров. Рекламодатели могут создавать посты по шаблону, которые публикуются в канале либо через общую очередь (бесплатно), либо в приоритетном порядке с выбором точного времени (платно).

## Основные возможности

- 📝 Конструктор постов с пошаговым заполнением
- 🕐 Две системы публикации: очередь и приоритетная
- 💾 Система черновиков
- ⏰ Автоматическая публикация по расписанию
- 💳 Интеграция с платежной системой (ЮКасса)
- ⚙️ Панель администратора с настройками

## Технический стек

### Backend
- **Python 3.11+**
- **aiogram 3.x** - фреймворк для Telegram ботов
- **PostgreSQL** - основная база данных
- **Redis** - кеширование и очереди задач
- **Celery** - фоновые задачи (публикация по расписанию)
- **SQLAlchemy** - ORM для работы с БД
- **Alembic** - миграции БД

### Платежи
- **ЮКасса API**

### DevOps
- **Docker** + **Docker Compose**

## Установка и запуск

### Требования

- Docker и Docker Compose
- Git

### Быстрый старт

1. **Клонирование репозитория:**

```bash
git clone https://github.com/yourusername/barter_marketplace.git
cd barter_marketplace
```

2. **Создание .env файла:**

```bash
cp .env.example .env
```

3. **Заполнение переменных окружения:**

Отредактируйте файл `.env` и заполните необходимые переменные:

```env
# Обязательные параметры
BOT_TOKEN=your_bot_token_from_botfather
ADMIN_IDS=123456789,987654321

# База данных (можно оставить по умолчанию)
DB_NAME=barter_bot
DB_USER=postgres
DB_PASSWORD=your_secure_password

# Платежная система (опционально, для приоритетных публикаций)
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key
```

4. **Запуск проекта:**

```bash
docker-compose up -d
```

5. **Просмотр логов:**

```bash
docker-compose logs -f bot
```

## Управление проектом

### Остановка проекта

```bash
docker-compose down
```

### Перезапуск проекта

```bash
docker-compose restart
```

### Применение миграций БД

```bash
docker-compose exec bot alembic upgrade head
```

### Просмотр логов конкретного сервиса

```bash
# Логи бота
docker-compose logs -f bot

# Логи Celery worker
docker-compose logs -f celery_worker

# Логи Celery beat (планировщик)
docker-compose logs -f celery_beat

# Логи PostgreSQL
docker-compose logs -f postgres

# Логи Redis
docker-compose logs -f redis
```

### Резервное копирование БД

```bash
# Создание backup
docker-compose exec postgres pg_dump -U postgres barter_bot > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановление из backup
docker-compose exec -T postgres psql -U postgres barter_bot < backup.sql
```

## Структура проекта

```
barter_marketplace/
├── docker-compose.yml          # Конфигурация Docker
├── Dockerfile                  # Образ Docker для бота
├── requirements.txt            # Python зависимости
├── .env.example               # Пример файла переменных окружения
├── .gitignore                 # Игнорируемые файлы
├── README.md                  # Документация
├── alembic/                   # Миграции базы данных
│   └── versions/
└── bot/                       # Исходный код бота
    ├── __init__.py
    ├── main.py               # Точка входа
    ├── config.py             # Конфигурация
    ├── database/             # Модели и работа с БД
    │   ├── __init__.py
    │   ├── models.py        # SQLAlchemy модели
    │   ├── database.py      # Подключение к БД
    │   └── crud.py          # CRUD операции
    ├── handlers/             # Обработчики команд и сообщений
    │   ├── __init__.py
    │   ├── start.py         # Команда /start
    │   ├── post_creator.py  # Создание постов
    │   └── admin.py         # Админ-панель
    ├── keyboards/            # Клавиатуры бота
    │   ├── __init__.py
    │   ├── main_menu.py
    │   └── post_creator.py
    ├── states/               # FSM состояния
    │   ├── __init__.py
    │   └── post_states.py
    ├── utils/                # Вспомогательные функции
    │   ├── __init__.py
    │   ├── post_formatter.py
    │   ├── duplicate_checker.py
    │   └── payments.py
    └── tasks/                # Celery задачи
        ├── __init__.py
        ├── celery_app.py
        └── publisher.py      # Автоматическая публикация
```

## Первоначальная настройка

После запуска бота выполните следующие шаги:

1. **Откройте бота в Telegram** и отправьте команду `/start`
2. **Для администраторов** появится дополнительное меню "⚙️ Панель администратора"
3. **Настройте канал для публикаций:**
   - Добавьте бота в администраторы вашего канала
   - Выдайте права на публикацию сообщений
   - В панели администратора выберите "📢 Настройки канала"
   - Отправьте username или ID канала

4. **Настройте расписание публикаций** (опционально):
   - Выберите "⏰ Расписание публикаций"
   - Укажите количество постов в день
   - Задайте время публикаций

5. **Настройте тарифы** (опционально):
   - Выберите "💰 Тарифы и цены"
   - Установите цены за публикации

## Использование

### Для рекламодателей

1. Отправьте команду `/start` боту
2. Выберите "📝 Создать пост"
3. Заполните все поля по шагам
4. Выберите тип публикации:
   - **Бесплатная очередь** - пост будет опубликован по порядку в установленное время
   - **Приоритетная публикация** (платная) - выберите точное время публикации

### Для администраторов

Доступ к панели администратора:
- 📢 Настройки канала - настройка канала для публикаций
- ⏰ Расписание публикаций - настройка времени и частоты публикаций
- 💰 Тарифы и цены - установка цен за публикации
- 📊 Статистика - просмотр статистики бота
- 📋 Очередь публикаций - управление очередью постов
- ⚡ Приоритетные публикации - просмотр платных публикаций

## Разработка

### Структура базы данных

Проект использует PostgreSQL со следующими таблицами:
- `users` - пользователи бота
- `posts` - посты (черновики, в очереди, опубликованные)
- `payments` - платежи
- `settings` - настройки бота
- `admin_logs` - логи действий администраторов

### Добавление новых обработчиков

1. Создайте новый файл в `bot/handlers/`
2. Определите роутер: `router = Router()`
3. Добавьте обработчики с декораторами
4. Зарегистрируйте роутер в `bot/main.py`:

```python
from bot.handlers import your_handler
dp.include_router(your_handler.router)
```

### Миграции базы данных

```bash
# Создание новой миграции
docker-compose exec bot alembic revision -m "description"

# Применение миграций
docker-compose exec bot alembic upgrade head

# Откат последней миграции
docker-compose exec bot alembic downgrade -1
```

## Мониторинг

### Просмотр состояния контейнеров

```bash
docker-compose ps
```

### Просмотр использования ресурсов

```bash
docker stats
```

## Решение проблем

### Бот не запускается

1. Проверьте логи: `docker-compose logs bot`
2. Убедитесь, что `BOT_TOKEN` корректен
3. Проверьте подключение к базе данных

### База данных недоступна

1. Проверьте статус контейнера: `docker-compose ps postgres`
2. Проверьте логи: `docker-compose logs postgres`
3. Убедитесь, что порт 5432 не занят

### Celery не публикует посты

1. Проверьте логи worker: `docker-compose logs celery_worker`
2. Проверьте логи beat: `docker-compose logs celery_beat`
3. Убедитесь, что Redis работает: `docker-compose ps redis`

## Поддержка

По вопросам и предложениям создавайте issue в репозитории проекта.

## Лицензия

MIT

## Авторы

Создано на основе спецификации бартерного бота для блоггеров.
